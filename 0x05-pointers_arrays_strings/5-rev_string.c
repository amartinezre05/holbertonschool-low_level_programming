#include "holberton.h"

/**
 * rev_string - Reverse a string.
 * @s: The pointer
 *
 * Return: To the value of void.
 */
void rev_string(char *s)
{
int i;
int cont;
char A[500];
cont = 0;
for (i = 0; s[i] != 0; i++)
{
}
cont = cont + i;
while (cont >= 0)
{
A[cont]= s[cont];
cont--;
}
}
